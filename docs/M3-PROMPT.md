# M3 hand-off prompt — Main flow (port the idea → ship pipeline)

Status: **prepared, not started.** Written after M2 (`abec939`). Milestone plan of record:
[`ADAPTATION_PLAN.md`](./ADAPTATION_PLAN.md) §6/M3. This file is a task prompt for the next
implementation session; it is not a decision and does not modify §5.

---

## Task: M3 — Main flow (port the idea → ship pipeline)

### Context

Repo: `C:/x/on/projs/pi-adapted-mp-skills` (Windows; bash available).
Package: `pi-adapted-mp-skills`, a port of Matt Pocock's engineering-workflow skill collection to the Pi coding agent.

**M0, M1, and M2 are complete and committed.** HEAD is `abec939` (`Add M2 convention layer: validator, agents, setup, router`), clean working tree. What exists now:

- `docs/skill-inventory.json` — the frozen adaptation contract: 38 upstream rows, `tokenMap` version 2, hash `9e86593bef1167039b66b4eb7d8dc69b5cda1cb647cde6e9fb086ad1e4bcda10`.
- `scripts/validate-skills.mjs` — dependency-free validator, currently exits 0 over `skills/**` (8 files, 2 `SKILL.md`).
- `agents/mp-researcher.md`, `mp-evidence-auditor.md`, `mp-review-standards.md`, `mp-review-spec.md` — the four package agents from M2 (D16).
- `skills/engineering/ask-matt/`, `skills/engineering/setup-matt-pocock-skills/` — the two M2 skills and their sidecars.
- No `docs/agents/`, no `.claude-plugin/`, no `extensions/` in this repo. No runtime dependencies.

**Read these first, in full:**

1. `docs/NOTES.md` — all three sections. **§M2 is binding context for this milestone**: the validator's checks and what it will now enforce on live cross-skill refs, the D14/background-child finding, the `pi.getCommands()` shadowing finding, and the interactive-skill-under-`pi -p` finding.
2. `docs/ADAPTATION_PLAN.md` — §1, §4, all of §5 (D1–D16, frozen), §6/M3, §7, §8. **Do not edit this file.**
3. `docs/skill-inventory.json` — the ten M3 rows in particular: `crossSkillLoads` (with `kind` and `d4Path`), `slashCommandReferences`, `harnessTokens`, `sidecarHarnessTokens`, `droppedFrontmatter`, `sidecars`, `piPath`.
4. Upstream at the pinned commit, cached at `../mp-upstream-c55ee46` (`c55ee46073ed923f86ce59a5eb3b6d895095d1b7`). Evidence, not content — never vendor it.
5. Pi docs: `C:/Users/LENOVO/AppData/Roaming/npm/node_modules/@earendil-works/pi-coding-agent/docs/` (`skills.md`, `settings.md`, `packages.md`, `extensions.md`, `usage.md`).
6. `pi-subagents` pinned `0.69.0` for the `subagent` tool surface — the cached tree is at `/c/Users/LENOVO/AppData/Local/Temp/tmp.lrhuS4YbIy/package` (docs: `docs/agents.md`, `docs/tool-reference.md`, `docs/configuration.md`; prompts: `prompts/parallel-review.md`). Re-fetch if the temp tree is gone. This is M3's first real dispatch site.

### Ground rules

- **D1–D16 are frozen. Do not re-open them. Do not edit `docs/ADAPTATION_PLAN.md` or any D5 row.**
- **Port only what §6/M3 lists.** Do not port M4/M5 skills even opportunistically. (One sequencing question below may be the sole exception — ask first.)
- Every ported file is a **fork-and-patch** (D9): copy the upstream file, apply the D5 map and the D2 frontmatter/`metadata`, and leave a per-file diff note in `CHANGELOG.md` or the M3 notes.
- **Substitution is total.** `Skill tool`, `Task tool`, `/clear`, `CLAUDE.md`, and `agents/openai.yaml` are banned with no allowlist, in bodies *and* sidecars.
- The M2 validator hard-fails on: D5 drift, unknown or dropped frontmatter keys, dangling or mis-named `.../SKILL.md` refs, forbidden tokens, user-invoked descriptions with model-facing trigger phrasing, duplicate names, and inventory mismatches (including **a recorded `d4Path` that the file does not use**). Do not weaken any check to make M3 pass; if a check is wrong, report it before changing it.
- Windows: forward-slashed, repo-relative paths in committed files; never embed `C:\`.
- M3 adds **no runtime dependencies**. `node scripts/validate-skills.mjs` must keep running on a bare Node install.

### Established facts (verified in M2 — do not re-derive)

- **`pi.getCommands()` reports only the winner of a name collision**; a shadowed copy is invisible. M2's setup skill pairs it with an on-disk scan. Relevant only if M3 touches collision handling (it should not).
- **Background (detached) children load ambient extensions; foreground children do not.** `async` defaults on for single-agent launches; the four `mp-*` agents set their own ceilings. `mp-review-*` are `read, grep, find, ls` only, so **they cannot run `git`** — the parent must hand them the diff.
- M2 proved `/skill:<name>` expands a skill body and that package skills are discovered with `origin: "package"`, `scope: "project"`. Use the same `before_agent_start` probe technique for discovery evidence.
- Interactive skills do **not** terminate under `pi -p` (no way to answer their questions; a pre-answered setup prompt made no writes and ran past 240 s). Plan the M3 acceptance accordingly.
- The four M2 agents consume a **contract**, not bundled prose. `mp-review-standards` already embeds the Fowler smell baseline; do not re-teach it in `code-review`.

### Deliverables (per §6/M3)

#### 1. Port the ten main-flow skills (copy → token map → D2 metadata → validator)

Exact `piPath`s from the inventory:

| Skill | Bucket | Invocation | Sidecars to port |
|---|---|---|---|
| `grill-with-docs` | engineering | user | `SKILL.md` |
| `grilling` | productivity | model | `SKILL.md` |
| `domain-modeling` | engineering | model | `SKILL.md`, `CONTEXT-FORMAT.md`, `ADR-FORMAT.md` |
| `to-spec` | engineering | user | `SKILL.md` |
| `to-tickets` | engineering | user | `SKILL.md` |
| `implement` | engineering | user | `SKILL.md` |
| `tdd` | engineering | model | `SKILL.md`, `tests.md`, `mocking.md` |
| `code-review` | engineering | model | `SKILL.md` |
| `prototype` | engineering | model | `SKILL.md`, `LOGIC.md`, `UI.md` |
| `handoff` | productivity | user | `SKILL.md` |

Every `agents/openai.yaml` is dropped; fold `invocation` into `metadata` (D2). `handoff`'s `argument-hint` is dropped (D2/D5-18) — its intent survives in the body's existing "if the user passed arguments…" sentence.

#### 2. Cross-skill loads use D4 (`d4Path` is mandatory)

The inventory records these, and the validator will require each to appear backticked:

- `grill-with-docs` → `` `../../productivity/grilling/SKILL.md` `` and `` `../domain-modeling/SKILL.md` `` (its whole body is one upstream line; it becomes two load instructions).
- `implement` → `` `../tdd/SKILL.md` `` and `` `../code-review/SKILL.md` `` (upstream `/tdd`, `/code-review`).
- `tdd` → `` `../codebase-design/SKILL.md` `` — **see the trap below.**

`to-spec`, `to-tickets`, and `code-review` reference `/setup-matt-pocock-skills` as a **user hand-off** (`kind: user-handoff`, `d4Path: null`): write *"tell the user to run `/skill:setup-matt-pocock-skills`"* — a label, never a load instruction. Keep `/skill:<name>` as a human-facing label everywhere; never phrase it as something the model executes.

#### 3. `code-review` dispatch contract — M3's first real `pi-subagents` site

This is the §6/M2 bullet ("define the exact `pi-subagents` calls the ported skills use") reaching its actual home. `code-review` is `adapt`: it must dispatch `mp-review-standards` and `mp-review-spec` **concurrently, in fresh isolated contexts, with read-only tools**, keep their findings separate, and synthesize only after both finish (D5-08). Requirements:

- Encode the **contract**, not orchestration. Name the two fixed axes, the required inputs, the fresh context, and "do not synthesize until both return". Do **not** copy `parallel-review.md` or re-teach fresh-vs-fork, angle selection, or run-identity mechanics — the bundled `pi-subagents` skill owns those (D16 items 2–4; where orchestration disagrees, the bundled prompt wins).
- Record, in the M3 notes, the exact calls used: agent names, foreground/background, context, how results are collected, and how a failed/aborted axis is reported (never a silently missing axis).
- **The diff cannot be run by the children.** Decide and document how the parent supplies it (e.g. capture `git diff <fixed-point>...HEAD` with `bash` into a repo path under `.scratch/` and pass the path; both agents have `read`). Upstream's "paste the diff command" assumption does not survive the read-only ceiling.
- If the spec/ticket is missing, the Spec axis is skipped and that is stated in the final report (upstream step 4).
- `grilling` also carries the `subagent` harness token ("dispatch a sub-agent to find facts"). See the second question below.

#### 4. `handoff` output relocation (D5-13)

Write `.scratch/handoffs/<ISO>-<slug>.md`, print the absolute path, keep it **git-ignored by default**; a repo may opt in to committing, never the reverse. Ensure the instruction covers adding the `.gitignore` pattern when it is not already covered. The body's "naming which skills the next agent should call the Skill tool for" must lose the literal `Skill tool` (D5-03) while keeping the "suggested skills" intent, now naming `/skill:<name>` labels.

#### 5. `docs/NOTES.md` — an `## M3` section; `CHANGELOG.md`

Append (do not touch M0/M1/M2) a section recording: per-file diff notes (D9); every deviation and plan silence with what you did instead; the `code-review` dispatch contract and its `subagent` calls; the `handoff` path decision; the `tdd`/`codebase-design` sequencing outcome; and the acceptance evidence. Update `CHANGELOG.md` following the M2 convention (landed files under `[0.1.0] → Added`; mark the M3 roadmap entry landed).

### Acceptance (from §6/M3)

- `node scripts/validate-skills.mjs` exits 0 with the D5 hash matching, over all M3 files.
- On a fixture repo **outside this repo**, run `/skill:grill-with-docs` → `/skill:to-spec` → `/skill:to-tickets` → `/skill:implement` on one ticket, **both** in one session and across split sessions, using only Pi tools. Because interactive skills do not terminate under `pi -p`, combine: (a) a discovery/visibility probe proving each skill loads with the right model-vs-user visibility and correct `/skill:<name>` resolution; (b) any real runs that do terminate; (c) a step-by-step structural verification of the flow's file outputs (spec, tickets, implementation). Say exactly which parts ran live and which were verified structurally, and why.
- Prove mechanically that every backticked `.../SKILL.md` reference under `skills/**` resolves and names the right skill — the first milestone where the set is non-empty, so report the enumeration, not just the validator's exit code.
- Confirm the `code-review` dispatch runs both axes fresh and read-only and does not synthesize before both finish (scripted or live, with the evidence shown).
- Confirm `handoff` writes under `.scratch/handoffs/` and that the path is git-ignored by default.

### Constraints and traps

- **Trap A — `tdd` loads `codebase-design`, which §6 assigns to M4.** With M3's validator, that recorded `d4Path` must appear in `tdd/SKILL.md` **and** `../codebase-design/SKILL.md` must exist. Porting `tdd` without `codebase-design` makes the validator fail. Ask me before choosing a resolution.
- **Trap B — `grilling` dispatches a sub-agent for fact-finding, but §1/D10 and D6's preflight list only `research`, `wayfinder`, `code-review`, `implement` as `pi-subagents`-gated.** Grilling is the shared primitive underneath `grill-with-docs`, which is part of the ungated main flow. Ask me before choosing a resolution.
- The review children are read-only; if you find yourself wanting to give them `bash` or `write`, stop and report instead.
- Do not add `docs/agents/`, `.claude-plugin/`, or `extensions/`.
- Do not rename skills or prefix `mp-` (D11). Names stay verbatim.
- `handoff` is user-invoked: its description must be a one-line summary, not trigger phrasing (validator check 5).
- `implement`'s `/tdd` and `/code-review` are **slash-loads** (D4): the agent must run them, so write the relative load, not just a label.

### Before you start, ask me

1. **`tdd` → `codebase-design` sequencing.** Options: **(A, recommended)** move `codebase-design` into M3 — a one-row `ownerMilestone` edit in `docs/skill-inventory.json` plus a note — because `tdd`'s load is a real model-load and M3's acceptance drives it; **(B)** keep M4 ownership and make the validator milestone-aware, allowing a `d4Path` whose target has an inventory row with a future `ownerMilestone` (weakens the existence assertion); **(C)** port `tdd` without the load and relax the inventory `d4Path` check for cross-milestone targets. I recommend A: it keeps the validator strict and the flow runnable, at the cost of a scheduling edit.
2. **`grilling`'s sub-agent dispatch.** Options: **(A, recommended)** rephrase the fact-finding to use the parent's own tools, so `grilling` stays ungated and §1/D10 and D6's capability list stay exactly as frozen; **(B)** gate `grilling` on `pi-subagents` too, which requires touching the frozen §1 list and the setup preflight (M2 code) — out of M3 scope. I recommend A, with the bundled `pi-subagents` skill owning any actual delegation if the parent chooses to delegate.

Also confirm: **should the M3 acceptance include a live run at all**, or is a discovery probe plus structural verification sufficient given the interactive-skill finding? (My recommendation: probe + structural, with any terminating live run reported as a bonus.)

Non-blocking and still open from earlier milestones: the GitHub owner `netname` in `README.md`/`NOTICE`/`CHANGELOG.md` (NOTES §M0.5 item 1), and the `git-guardrails` extension-packaging question (NOTES §M1.6 item 3), which is M5 work.

### When done

Report in this shape:

- Files created/changed (paths + one-line purpose each)
- The validator result (file/SKILL.md counts) and the D5 freeze result
- The ten ported skills, with per-file diff notes and the exact D4 references written
- The `code-review` dispatch contract: the exact `subagent` calls, context, result collection, and failure handling
- The `tdd`/`codebase-design` resolution and the `grilling` dispatch decision, each with its evidence
- The `handoff` output path and git-ignore behavior
- The fixture-repo flow evidence: what ran live vs. structurally verified, with commands and outputs
- Every plan silence or error you hit, with what you did instead
- Confirmation that D1–D16 were not altered, and that `docs/ADAPTATION_PLAN.md` and the D5 table are byte-identical to `abec939`
