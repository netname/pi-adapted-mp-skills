# M4 hand-off prompt — On-ramps, discovery, and health

Status: **prepared, not started.** Written after M3 (`7a582db`). Milestone plan of record:
[`ADAPTATION_PLAN.md`](./ADAPTATION_PLAN.md) §6/M4. This file is a task prompt for the next
implementation session; it is not a decision and does not modify §5.

---

## Task: M4 — On-ramps, discovery, and health (`wayfinder`, `research`, `triage`, `diagnosing-bugs`, `improve-codebase-architecture`, `resolving-merge-conflicts`, `wizard`)

### Context

Repo: `C:/x/on/projs/pi-adapted-mp-skills` (Windows; bash available).
Package: `pi-adapted-mp-skills`, a port of Matt Pocock's engineering-workflow skill collection to the Pi coding agent.

**M0–M3 are complete and committed.** HEAD is `7a582db` (`Add M3 main flow: ten skills, dispatch contract, handoff relocation`), clean working tree. What exists now:

- `docs/skill-inventory.json` — the frozen adaptation contract: 38 upstream rows, `tokenMap` version 2, hash `9e86593bef1167039b66b4eb7d8dc69b5cda1cb647cde6e9fb086ad1e4bcda10`. The only M3 edit is the `codebase-design` row's `ownerMilestone` (M4 → M3) plus a note.
- `scripts/validate-skills.mjs` — dependency-free validator; exits 0 over `skills/**` (27 files, 13 `SKILL.md`) and now exercises a **non-empty** cross-skill set (5 backticked `../…/SKILL.md` references).
- `agents/mp-researcher.md`, `mp-evidence-auditor.md`, `mp-review-standards.md`, `mp-review-spec.md` — the four package agents (D16).
- 13 ported skills: the two M2 ones plus the M3 main flow and `codebase-design`.
- No `docs/agents/`, no `.claude-plugin/`, no `extensions/` in this repo. No runtime dependencies.

**Read these first, in full:**

1. `docs/NOTES.md` — all four sections. **§M3 is binding context**: the code-review dispatch contract as actually run, the `pi install` backslash-path defect on Windows, the pre-spawn task-classifier refusal, the visibility-vs-availability distinction, and the corrected interactive-skill finding.
2. `docs/ADAPTATION_PLAN.md` — §1, §4, all of §5 (D1–D16, frozen), §6/M4, §7, §8, §10. **Do not edit this file.**
3. `docs/skill-inventory.json` — the seven M4 rows in particular: `crossSkillLoads` (with `kind` and `d4Path`), `slashCommandReferences`, `harnessTokens`, `sidecarHarnessTokens`, `droppedFrontmatter`, `sidecars`, `piPath`.
4. Upstream at the pinned commit, cached at `../mp-upstream-c55ee46` (`c55ee46073ed923f86ce59a5eb3b6d895095d1b7`). Evidence, not content — never vendor it.
5. Pi docs: `C:/Users/LENOVO/AppData/Roaming/npm/node_modules/@earendil-works/pi-coding-agent/docs/` (`skills.md`, `settings.md`, `packages.md`, `extensions.md`, `usage.md`).
6. `pi-subagents` pinned `0.69.0`: `docs/agents.md`, `docs/tool-reference.md`, `docs/configuration.md`, `prompts/parallel-research.md`, `prompts/gather-context-and-clarify.md`. Install it into the **fixture**, not this repo.

### Ground rules

- **D1–D16 are frozen. Do not re-open them. Do not edit `docs/ADAPTATION_PLAN.md` or any D5 row.**
- **Port only what §6/M4 lists** (seven skills; `codebase-design` already shipped in M3). Do not port M5 skills opportunistically.
- Every ported file is a **fork-and-patch** (D9): copy the upstream file, apply the D5 map and the D2 frontmatter/`metadata`, and leave a per-file diff note in `CHANGELOG.md` or the M4 notes.
- **Substitution is total.** `Skill tool`, `Task tool`, `/clear`, `CLAUDE.md`, and `agents/openai.yaml` are banned with no allowlist, in bodies *and* sidecars. `wayfinder` is the trap: its `**Task** (HITL or AFK)` is a ticket **type**, not the Task tool, and the validator only matches the literal `Task tool`.
- The M3 validator hard-fails on: D5 drift, unknown or dropped frontmatter keys, dangling or mis-named `.../SKILL.md` refs, forbidden tokens, user-invoked descriptions with model-facing trigger phrasing, duplicate names, and inventory mismatches (including **a recorded `d4Path` that the file does not use**). Do not weaken any check to make M4 pass; if a check is wrong, report it before changing it.
- **Do not widen any `mp-*` agent's tool ceiling** to add `bash`, `git`, or `edit` without asking (see Trap A).
- Windows: forward-slashed, repo-relative paths in committed files; never embed `C:\`.
- M4 adds **no runtime dependencies**. `node scripts/validate-skills.mjs` must keep running on a bare Node install.

### Established facts (verified in M3 — do not re-derive)

- **The M2 "interactive skills do not terminate under `pi -p`" finding holds for `setup-matt-pocock-skills`, not for the main flow.** `grill-with-docs`, `to-spec`, `to-tickets`, `implement`, `code-review`, and `handoff` all terminated live; a skill that ends its turn with a question still exits 0. Do not assume M4's interactive skills must be probed-only — try a live run first and fall back if it does not terminate.
- **Model visibility must be read from the rendered `<available_skills>` block, not from `systemPromptOptions.skills`.** The latter lists *all* registered skills including `disable-model-invocation: true` ones, so it answers "is it available?", not "can the model choose it?".
- **On Windows, `pi install <path> -l` writes a backslash-relative package path** (`"..\\..\\pi-adapted-mp-skills"`), and `pi-subagents@0.69.0` resolves only forward-slash relative sources, so this package's `pi.subagents.agents` manifest is never read and **zero `mp-*` agents resolve**. Before any dispatch in the fixture, set the package entry to the forward-slash form (`"../../pi-adapted-mp-skills"`) and confirm with `discoverAgents()`; full evidence is in `docs/NOTES.md` §M3.5. The README troubleshooting note for this is M6 work.
- **The dispatch shape that worked** was a single `subagent` call with `async: true`, `context: "fresh"`, and a `workflowScript` using `await runs.all([{ key, agent, task, output }])`; the `output:` field gives each child a durable artifact path the parent then `read`s. Per D16, encode the requirement in the ported skill and let the bundled `pi-subagents` skill own the mechanics.
- **`pi-subagents`' pre-spawn task classifier can refuse a read-only agent** described as an implementation task (`Agent '…' was given an implementation task, but its tool allowlist has no mutation-capable tools` — 0 turns, exit 1). The parent must phrase read-only work as an audit/review, and must report a refused launch as *not completed* rather than silently dropping it.
- **Background (detached) children load ambient extensions; foreground children do not.** `mp-researcher` and `mp-evidence-auditor` declare `async: true`, so they inherit `pi-web-access` ambiently; `mp-review-*` stay foreground and read-only.
- **Tool ceilings that matter here:** `mp-researcher` is `read, write, web_search, fetch_content, get_search_content, source_check` — **no `bash`**, so it cannot create a branch or commit. `mp-evidence-auditor` is `read, web_search, fetch_content, get_search_content, source_check` — **no `write` and no `bash`**, so its output is returned inline and the parent must persist it.
- The four `mp-*` agents consume a **contract**, not bundled prose (D16). Do not re-teach fresh-vs-fork, angle selection, run identity, or the researcher/scout split.

### Deliverables (per §6/M4)

#### 1. Port the seven M4 skills (copy → token map → D2 metadata → validator)

Exact `piPath`s from the inventory:

| Skill | Bucket | Invocation | Disposition | Sidecars to port |
|---|---|---|---|---|
| `wayfinder` | engineering | user | port | `SKILL.md` |
| `research` | engineering | model | **adapt** | `SKILL.md` |
| `triage` | engineering | user | port | `SKILL.md`, `AGENT-BRIEF.md`, `OUT-OF-SCOPE.md` |
| `diagnosing-bugs` | engineering | model | port | `SKILL.md`, `scripts/hitl-loop.template.sh` |
| `improve-codebase-architecture` | engineering | user | port | `SKILL.md`, `HTML-REPORT.md` |
| `resolving-merge-conflicts` | engineering | model | port | `SKILL.md` |
| `wizard` | engineering | model | port | `SKILL.md`, `template.sh` |

12 files. Every `agents/openai.yaml` is dropped; fold `invocation` into `metadata` (D2). `resolving-merge-conflicts` is already harness-neutral — port it verbatim plus metadata. The three user-invoked descriptions (`wayfinder`, `triage`, `improve-codebase-architecture`) must stay one-line summaries, not trigger phrasing (validator check 5).

#### 2. Cross-skill loads use D4 (9 recorded paths; `d4Path` is mandatory)

Every recorded `d4Path` must appear backticked, and the validator will resolve each one and compare the target's `name`:

- `triage` → `` `../../productivity/grilling/SKILL.md` `` and `` `../domain-modeling/SKILL.md` ``; plus the user hand-off label "tell the user to run `/skill:setup-matt-pocock-skills`" (`kind: user-handoff`, `d4Path: null`).
- `wayfinder` → `` `../../productivity/grilling/SKILL.md` ``, `` `../domain-modeling/SKILL.md` ``, `` `../research/SKILL.md` ``, `` `../prototype/SKILL.md` ``; plus the user hand-off label for setup.
- `improve-codebase-architecture` → `` `../codebase-design/SKILL.md` `` (shipped in M3), `` `../../productivity/grilling/SKILL.md` ``, `` `../domain-modeling/SKILL.md` ``; plus the bare `` `/codebase-design` `` prose reference (line ~54) as a `/skill:codebase-design` label.

**There is no cross-milestone trap this time:** every target (`research`, `prototype`, `codebase-design`, `grilling`, `domain-modeling`) exists by the end of M4. Prove it by enumerating all backticked `../…/SKILL.md` references again (`validate-skills.mjs` only checks resolution; report the enumeration).

Occurrence details to get right, per file:

- `wayfinder` is the richest case: the "Call the Skill tool twice, for \"grilling\" and \"domain-modeling\"" sentence appears **three times** (ticket-type list ~line 79, chart-the-map step 1 ~line 111, work-the-map step 3 ~line 124), and each occurrence must become **two** load instructions (one skill per instruction). The research load appears twice (~lines 77 and 115) and the prototype load once (~line 78). Line 124 also has a **generic** "call the Skill tool for whichever skills the `## Notes` block names" with no fixed target — rewrite that one as "read that skill's `SKILL.md` and follow it" without inventing a `d4Path` (a dynamic target is not a recorded load).
- `research` is 12 lines and its whole body is harness prose ("Spin up a **background agent**") — this is the `adapt`, see deliverable 3.
- `triage/AGENT-BRIEF.md` line ~25 contains `` `/triage` `` inside a sample user story; rewrite it to `/skill:triage` (D5-16), not a load instruction.
- `wizard/template.sh` line 4 comment says "Generated by the /wizard skill." → `/skill:wizard`.
- `diagnosing-bugs` has **no** cross-skill load. §5/D4 claims it hands off to `improve-codebase-architecture`, but the pinned file contains no such reference (M1 finding). Do not add one.

#### 3. `research` + `wayfinder` — the M4 `pi-subagents` dispatch sites

`research` and `wayfinder` are the two skills that must delegate (D5's Background research agent and Parallel research frontier rows). Encode the **contract**, not the orchestration:

- `research` (adapt): dispatch **one detached `mp-researcher` child**; verify before launch that `web_search`, `fetch_content`, `get_search_content`, and `source_check` are registered, with the pinned `pi-web-access@0.29.0` remedy when they are not (D10); give the child a **unique, explicit output path**; save its run identity; collect its result before the question is considered answered; a failed or abandoned child leaves the question unresolved and visible.
- `wayfinder`: for each independent `research` ticket, fire **one bounded `mp-researcher` child**, all in a single concurrent batch; never resolve a ticket without its child's evidence; if a child fails, record the failure and leave the ticket open.
- **Children must not edit shared map or ticket files.** They return evidence or write unique research artifacts; the **parent serializes** every tracker and map update. Assign each child a distinct output path so two concurrent children cannot collide.
- Do **not** copy `parallel-research.md` or restate fresh-vs-fork, angle selection, or run identity; the bundled `pi-subagents` skill owns those (D16).
- Record in the M4 notes the exact calls used: agent names, foreground/background, context, concurrency, output paths, result collection, and failure handling.

#### 4. `improve-codebase-architecture` report relocation (D5-14)

The HTML report moves out of the OS temp directory to `.scratch/reports/<ISO>-architecture.html`, **git-ignored by default**, with the absolute path printed for the user. Keep the "open it" step (`xdg-open` / `open` / `start`). The `$TMPDIR` / `/tmp` / `%TEMP%` resolution paragraph is replaced by the repo path. `HTML-REPORT.md`'s Tailwind/Mermaid CDN links are third-party runtime fetches by the generated HTML, not harness tokens — leave them, and do not add `.scratch/reports/` to any committed ignore file in *this* package (it is target-repo output; setup already covers it).

#### 5. `wizard` and `diagnosing-bugs` shell templates

Keep `wizard/template.sh` and `diagnosing-bugs/scripts/hitl-loop.template.sh` as shell templates; do not rewrite them in JS. `wizard/template.sh` gets the `/skill:wizard` comment fix. Verify each runs under Pi's `bash` (`bash -n`, and `shellcheck` if available) and **record the Windows limits** in the skill or the M4 notes (the templates use `mktemp`, `xdg-open`/`open`/`start`, and `gh`). The upstream `$TMPDIR`/`localhost:3000`/Stripe-URL references are fine (dev-server defaults and examples), but the harness path decisions are not.

#### 6. `triage`

Keep the five label roles mapping to the target repo's `docs/agents/triage-labels.md` (setup's output, not package content — never create `docs/agents/` here). Exercise the local-Markdown path at least; note `gh`/`glab` coverage as M6 work. `AGENT-BRIEF.md` keeps its sample acceptance criterion but with the `/skill:triage` label.

#### 7. `mp-evidence-auditor` vs built-in `evidence-auditor` (§6/M4, D16 item 5)

Run both against the same claim/sources once and record the comparison. If they are functionally indistinguishable in practice, collapse to the built-in and record the change under D16 item 5; otherwise keep the custom agent and record why. This is a scheduled M4 decision, not an optional extra.

#### 8. `docs/NOTES.md` — an `## M4` section; `CHANGELOG.md`

Append (do not touch M0–M3) a section recording: per-file diff notes (D9); every deviation and plan silence with what you did instead; the `research`/`wayfinder` dispatch contract and its `subagent` calls; the report-relocation decision; the wizard/diagnosing-bugs Windows note; the `evidence-auditor` comparison and its outcome; and the acceptance evidence. Update `CHANGELOG.md` following the M2/M3 convention (landed files under `[0.1.0] → Added`; mark the M4 roadmap entry landed).

### Acceptance (from §6/M4, made concrete)

- `node scripts/validate-skills.mjs` exits 0 with the D5 hash matching, over all M4 files; report the file/`SKILL.md` counts.
- Enumerate every backticked `.../SKILL.md` reference under `skills/**` (now 9 recorded paths, more occurrences) and show each resolves to a target whose `name` matches.
- Each of the seven skills loads on a fixture repo **outside this repo**, with the right model-vs-user visibility, and `/skill:<name>` resolves for the three user-invoked ones (`wayfinder`, `triage`, `improve-codebase-architecture`). Use the `before_agent_start` probe technique from M2/M3, reading visibility from the rendered `<available_skills>` block.
- `research` dispatches a detached `mp-researcher` child that returns a cited note; show a failed/refused child leaving the question unresolved.
- `wayfinder` fires independent research tickets concurrently and the parent serializes tracker writes; show the concurrent batch and the ticket/map updates.
- `triage` completes at least the local-Markdown path (labels mapped to `docs/agents/triage-labels.md`).
- `improve-codebase-architecture` writes under `.scratch/reports/`, prints the absolute path, and the file is untracked by `git status`.
- `wizard`/`diagnosing-bugs` templates pass `bash -n`, with a recorded Windows note.
- The `mp-evidence-auditor`/`evidence-auditor` comparison is recorded with its outcome.

Report what ran live and what was verified structurally, and why.

### Constraints and traps

- **Trap A — `wayfinder`'s throwaway `research/<name>` branch needs `git`, but `mp-researcher` has no `bash`.** Upstream line ~115: "spin up a subagent that calls the Skill tool with \"research\" … capturing its findings on a throwaway `research/<name>` branch with a context pointer from the ticket." The child can write the note file but cannot branch or commit. Ask before choosing a resolution (question 1 below).
- **Trap B — the pre-spawn task classifier refuses read-only agents described as implementation tasks** (M3 evidence: `mp-review-standards`, 0 turns, exit 1). `mp-evidence-auditor` has no `write` and no `bash`, so it is the exposed agent here. Phrase its task as an audit, and treat a refusal as a reported failure with a retry, never a silently missing result.
- **Trap C — concurrent research children and shared files.** `mp-researcher`'s own brief tells it to choose a path "where the repo already keeps such notes", so two children can pick the same file. Assign explicit unique `output:` paths; keep map/ticket writes parent-only.
- Do not widen any `mp-*` tool ceiling (`bash`, `edit`, `git`) without asking.
- Do not add `docs/agents/`, `.claude-plugin/`, or `extensions/`.
- Do not rename skills or prefix `mp-` (D11). Names stay verbatim.
- `improve-codebase-architecture`'s report is target-repo output and git-ignored by default; a repo may opt in, never the reverse.

### Before you start, ask me

1. **`wayfinder`'s throwaway `research/<name>` branch vs a child with no `bash`.** Options: **(A, recommended)** the parent creates the branch and commits the child's note, leaving the child to write files only — the upstream intent (a durable primary-source artifact) survives, and no ceiling changes; **(B)** widen `mp-researcher` to include `bash` so it can branch and commit itself — changes the M2 agent contract and its safety ceiling; **(C)** drop the throwaway branch and keep notes under `.scratch/research/<name>.md`, recording the deviation — simplest, but loses upstream's "primary source out of main" property. I recommend A.
2. **`mp-evidence-auditor` vs built-in `evidence-auditor`** (§6/M4, D16 item 5). Options: **(A, recommended)** run both once on the same claim/sources, compare, and keep the custom agent unless they are indistinguishable — recording the comparison either way; **(B)** collapse to the built-in now, on the theory that the guide's verification step is the same shape. I recommend A, because the guide's check is explicitly claim-vs-specific-source rather than a general second opinion, but the evidence decides.
3. **M4 acceptance live scope.** Options: **(A, recommended)** probe the seven skills, plus one **live** detached `mp-researcher` dispatch, one **live** `mp-evidence-auditor` run, and a live-or-scripted `wayfinder` concurrency check (needs network and pinned `pi-web-access@0.29.0` in the fixture); **(B)** probe + structural verification only. I recommend A: the M3 live runs were cheap and far more informative than the structural checks, and `research` is entirely about the child launch.

Non-blocking, please confirm the approach: `wayfinder`'s generic "call the Skill tool for whichever skills the `## Notes` block names" has no fixed target. I plan to rewrite it as "read that skill's `SKILL.md` and follow it", without inventing a `d4Path` — say if you'd rather drop the clause.

Non-blocking and still open from earlier milestones: the GitHub owner `netname` in `README.md`/`NOTICE`/`CHANGELOG.md` (NOTES §M0.5 item 1); the `pi-subagents` Windows backslash-path README entry (NOTES §M3.5), which is M6 work; and the `git-guardrails` extension-packaging question (NOTES §M1.6 item 3), which is M5 work.

### When done

Report in this shape:

- Files created/changed (paths + one-line purpose each)
- The validator result (file/`SKILL.md` counts) and the D5 freeze result
- The seven ported skills, with per-file diff notes and the **9 enumerated** D4 references written
- The `research`/`wayfinder` dispatch contract: the exact `subagent` calls, context, concurrency, output paths, result collection, and failure handling
- The `wayfinder` throwaway-branch resolution and the `evidence-auditor` comparison, each with its evidence
- The `improve-codebase-architecture` report path and git-ignore behavior
- The wizard/diagnosing-bugs `bash -n` results and the recorded Windows limits
- The fixture-repo evidence: what ran live vs. structurally verified, with commands and outputs
- Every plan silence or error you hit, with what you did instead
- Confirmation that D1–D16 were not altered, and that `docs/ADAPTATION_PLAN.md` and the D5 table are byte-identical to `7a582db`
