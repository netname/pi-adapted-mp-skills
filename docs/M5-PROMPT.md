# M5 hand-off prompt — Productivity and misc

Status: **prepared, not started.** Written after M4 (`fca16af`). Milestone plan of record:
[`ADAPTATION_PLAN.md`](./ADAPTATION_PLAN.md) §6/M5. This file is a task prompt for the next
implementation session; it is not a decision and does not modify §5.

---

## Task: M5 — Productivity and misc (`grill-me`, `to-questionnaire`, `wait-what`, `writing-for-agents`, `teach`, `git-guardrails`)

### Context

Repo: `C:/x/on/projs/pi-adapted-mp-skills` (Windows; bash available).
Package: `pi-adapted-mp-skills`, a port of Matt Pocock's engineering-workflow skill collection to the Pi coding agent.

**M0–M4 are complete and committed.** HEAD is `fca16af` (`Add M4 on-ramps and health: seven skills,
research dispatch contract, report relocation`), clean working tree. What exists now:

- `docs/skill-inventory.json` — the frozen adaptation contract: 38 upstream rows, `tokenMap` version 2, hash `9e86593bef1167039b66b4eb7d8dc69b5cda1cb647cde6e9fb086ad1e4bcda10`. Byte-identical to `7a582db` (M4 touched nothing in it).
- `scripts/validate-skills.mjs` — dependency-free validator; exits 0 over `skills/**` (**39 files, 20 `SKILL.md`**) with a non-empty cross-skill set (20 backticked `../…/SKILL.md` occurrences across 7 distinct targets).
- `agents/mp-researcher.md`, `mp-evidence-auditor.md`, `mp-review-standards.md`, `mp-review-spec.md` — the four package agents (D16). M4 kept `mp-evidence-auditor` and scheduled a discriminating re-test for M6 (NOTES §M4.8).
- **20 ported skills**: the two M2 ones, the M3 main flow + `codebase-design`, and the seven M4 on-ramps/health skills.
- No `docs/agents/`, no `.claude-plugin/`, **no `extensions/`**, and no `pi.extensions` in `package.json`. No runtime dependencies.

M5 is the **last porting milestone**: after it, all 26 v1 skills ship. M6 is validation/docs/release only.

**Read these first, in full:**

1. `docs/NOTES.md` — all five sections. **§M4 is the binding context for this milestone**: the D4 enumeration, the `research`/`wayfinder` dispatch contract, the `output:` routing finding, the report relocation, the Windows template limits, the Trap B refusal evidence, and the evidence-auditor comparison. §M3.5 (backslash-path defect) and §M1.6 item 3 (`git-guardrails` packaging) are still open and land here or in M6.
2. `docs/ADAPTATION_PLAN.md` — §1, §4, all of §5 (D1–D16, frozen), §6/M5, §7, §8, §10. **Do not edit this file.**
3. `docs/skill-inventory.json` — the six M5 rows in particular: `crossSkillLoads`, `slashCommandReferences`, `harnessTokens`, `sidecarHarnessTokens`, `droppedFrontmatter`, `sidecars`, `piPath`, and for `git-guardrails-claude-code` the `disposition: "rename"` + `renameTarget: "git-guardrails"`.
4. Upstream at the pinned commit, cached at `../mp-upstream-c55ee46` (`c55ee46073ed923f86ce59a5eb3b6d895095d1b7`). Evidence, not content — never vendor it.
5. Pi docs: `C:/Users/LENOVO/AppData/Roaming/npm/node_modules/@earendil-works/pi-coding-agent/docs/` (`skills.md`, `settings.md`, `packages.md`, `extensions.md`, `usage.md`). `packages.md` shows the `pi.extensions` manifest shape; `extensions.md` documents `pi.on("tool_call", …)` returning `{ block: true, reason }`.
6. Pi bundled extension examples: `…/@earendil-works/pi-coding-agent/examples/extensions/permission-gate.ts` and `protected-paths.ts` — the two working `tool_call` blockers the `git-guardrails` port should mirror.
7. `pi-subagents` pinned `0.69.0` is **not** needed by any M5 skill (none dispatches a child). Do not install it into M5's fixture unless a check needs it.

### Ground rules

- **D1–D16 are frozen. Do not re-open them. Do not edit `docs/ADAPTATION_PLAN.md` or any D5 row.**
- **Port only what §6/M5 lists** (six skills). `setup-pre-commit` is **not** ported in v1 (D15) — do not port it, do not add it to the tree, do not add its validator check. Do not port `in-progress/*` or `migrate-to-shoehorn`/`scaffold-exercises`.
- Every ported file is a **fork-and-patch** (D9): copy the upstream file, apply the D5 map and the D2 frontmatter/`metadata`, and leave a per-file diff note in `CHANGELOG.md` or the M5 notes.
- **Substitution is total.** `Skill tool`, `Task tool`, `/clear`, `CLAUDE.md`, and `agents/openai.yaml` are banned with no allowlist, in bodies *and* sidecars.
- The M4 validator hard-fails on: D5 drift, unknown or dropped frontmatter keys, dangling or mis-named `.../SKILL.md` refs, forbidden tokens, user-invoked descriptions with model-facing trigger phrasing, duplicate names, and inventory mismatches (including **a recorded `d4Path` that the file does not use**). Do not weaken any check to make M5 pass; if a check is wrong, report it before changing it (Trap A is exactly this).
- Windows: forward-slashed, repo-relative paths in committed files; never embed `C:\`.
- M5 adds **no runtime dependencies**. `node scripts/validate-skills.mjs` must keep running on a bare Node install.

### Established facts (verified in M3/M4 — do not re-derive)

- **`/skill:<name>` expansion works for user-invoked skills and they terminate under `pi -p`.** All seven M4 skills ran live and exited 0. The only skill that never terminated was `setup-matt-pocock-skills` (M2). So M5's four user-invoked skills should be **run live**, not assumed to be probe-only.
- **Model visibility must be read from the rendered `<available_skills>` block, not from `systemPromptOptions.skills`.** The latter lists *all* registered skills, including `disable-model-invocation: true` ones.
- **`pi install <path> -l` on Windows writes a backslash-relative package path**, and `pi install … -l` refuses npm packages until run with `--approve`. Repair the package entry to forward slashes before any probe; the README note for this is M6 work.
- **A read-only agent given an implementation task is refused pre-spawn** ("… has no mutation-capable tools"), and a foreground child cannot load ambient extension tools — use `async: true` for any extension-tool child. No M5 skill dispatches a child, so this is context only.
- **Foreground children do not load ambient extensions; background children do.** Relevant to the `git-guardrails` extension decision: a guardrail must be loaded by the **parent** session (`pi.extensions` / settings), not by a child.
- **No M5 skill carries a `d4Path` except `grill-me`.** The set is otherwise empty; do not invent loads.
- The ported skills consume **contracts**, not bundled prose. Do not restate `pi-subagents` mechanics anywhere.

### Deliverables (per §6/M5)

#### 1. Port the six M5 skills (copy → token map → D2 metadata → validator)

Exact `piPath`s from the inventory:

| Skill | Bucket | Invocation | Disposition | Sidecars to port |
|---|---|---|---|---|
| `grill-me` | productivity | user | port | `SKILL.md` |
| `to-questionnaire` | productivity | user | port | `SKILL.md` |
| `wait-what` | productivity | user | port | `SKILL.md` |
| `writing-for-agents` | productivity | model | port | `SKILL.md`, `SKILL-MECHANICS.md` |
| `teach` | productivity | user | port | `SKILL.md`, `MISSION-FORMAT.md`, `GLOSSARY-FORMAT.md`, `LEARNING-RECORD-FORMAT.md`, `RESOURCES-FORMAT.md` |
| `git-guardrails` | misc | model | **rename** (from `git-guardrails-claude-code`) | `SKILL.md`, `scripts/block-dangerous-git.sh` |

12 files. `skills/misc/` is created here for the first time. Every `agents/openai.yaml` is dropped;
fold `invocation` into `metadata` (D2). Expected validator counts after M5: **51 files / 26
`SKILL.md`** (plus one file if the `git-guardrails` extension route adds `extensions/*.ts`). The four
user-invoked descriptions (`grill-me`, `to-questionnaire`, `wait-what`, `teach`) must stay one-line
summaries, not trigger phrasing (validator check 5); `writing-for-agents` and `git-guardrails` are
model-invoked and keep trigger phrasing.

#### 2. `grill-me` — the only M5 cross-skill load

Upstream is a 7-line skill whose entire body is ``Call the Skill tool with "grilling".`` Rewrite it
as the D4 **same-bucket** load, one skill per instruction:

> Load the `grilling` skill: read `../grilling/SKILL.md` and follow it.

The validator will resolve it from `skills/productivity/grill-me/` to
`skills/productivity/grilling/SKILL.md` and assert the target's `name` is `grilling`. This is a new
**occurrence** of an existing distinct path, so the enumeration grows to 21 occurrences / 7 distinct
paths — report it.

#### 3. `git-guardrails` — rename, mechanism, and packaging (D3, D5-20, §10)

This is the milestone's real architectural decision and it has **three separate problems**:

1. **The rename.** D3 freezes `git-guardrails-claude-code` → `git-guardrails`. The inventory row has
   `disposition: "rename"` and `renameTarget: "git-guardrails"`, but `upstreamName` stays
   `git-guardrails-claude-code` (the inventory is frozen). The validator's check 7 compares
   `frontmatterName !== row.upstreamName`, so `name: git-guardrails` will **hard-fail**. See Trap A.
2. **The mechanism.** The pinned skill is wholly Claude Code: a `PreToolUse` hook matcher on `Bash`,
   `.claude/settings.json`, `.claude/hooks/`, `~/.claude/hooks/`, `$CLAUDE_PROJECT_DIR`, and a bash
   script that reads the tool input as JSON on stdin (via `jq`) and exits 2. Pi's equivalent is a
   `pi.on("tool_call", …)` extension returning `{ block: true, reason }` (D5-20). Port the **blocked
   pattern list and the block/allow intent**, not the hook JSON. The `jq`/stdin/exit-2 contract must
   not survive.
3. **The packaging.** D1's frozen `pi` manifest declares only `skills` and `subagents`, and §4's tree
   has no `extensions/`. §10 and the D5-20 row nonetheless say "packaging the extension is M5 work".
   So M5 must choose how the guardrail actually gets installed. See the question below.

Whichever route is chosen, the skill stays **model-invoked**, keeps a model-facing description, and
must not widen any `mp-*` ceiling (it has none — it is a skill, not an agent). Its `scripts/` sidecar
either stays as the pattern source or is replaced by the TS patterns; record which.

#### 4. `writing-for-agents` — `CLAUDE.md` and Pi invocation mechanics

- The validator bans the literal `CLAUDE.md` with no allowlist (D5-15), and the upstream
  `description` **and** body both contain it. Rewrite the description to name `AGENTS.md` (keeping
  model-facing trigger phrasing) and the body's "an `AGENTS.md` / `CLAUDE.md`" pairing to `AGENTS.md`.
- `SKILL-MECHANICS.md` describes the model-vs-user invocation split in Claude/Codex vocabulary. It
  describes the same split Pi implements, so reconcile the **field names** with Pi's frontmatter
  (`disable-model-invocation`, `description`) and drop any `policy.allow_implicit_invocation`-style
  phrasing. Keep it a reference doc (D7).
- The row's `harnessTokens` also lists `subagent` (the body's "a hand-off or a subagent dispatch"
  context-boundary sentence). Confirm whether to name the `subagent` tool there or leave the generic
  wording — see the non-blocking note.

#### 5. `teach` — `argument-hint` and the four FORMAT sidecars

Drop `argument-hint` (D5-18/D2); the validator hard-fails on it. Its intent is already carried by the
body. Port all four `*-FORMAT.md` sidecars verbatim with the skill-relative `[MISSION-FORMAT.md](./MISSION-FORMAT.md)`
style links intact (D7). `teach` ships **experimental** per D15 — the README index already marks it
`(experimental)` at line ~156, so the only README work is to leave that mark in place (M6 finalizes
the index). `RESOURCES-FORMAT.md`'s `https://example.com` / `reddit.com/r/weightroom` URLs are
illustrative examples, not harness tokens — leave them.

#### 6. `to-questionnaire` and `wait-what` — verbatim plus metadata

Neither carries a harness token. `to-questionnaire` writes `to-questionnaire-<slug>.md` in the current
directory (no D5 relocation is scheduled — keep it; it is target-repo output). `wait-what` references
`CONTEXT.md` / `CONTEXT-MAP.md`, which are target-repo conventions, not tokens. Port both verbatim
plus the D2 metadata.

#### 7. `docs/NOTES.md` — an `## M5` section; `CHANGELOG.md`

Append (do not touch M0–M4) a section recording: per-file diff notes (D9); the rename resolution and
the validator change (if any); the `git-guardrails` mechanism and packaging decision with the live
block evidence; the `writing-for-agents` invocation reconciliation; every deviation and plan silence
with what you did instead; and the acceptance evidence. Update `CHANGELOG.md` following the M2–M4
convention (landed files under `[0.1.0] → Added`; mark the M5 roadmap entry landed).

### Acceptance (from §6/M5, made concrete)

- `node scripts/validate-skills.mjs` exits 0 with the D5 hash matching, over all M5 files; report the
  file/`SKILL.md` counts (expected **51 / 26**, or 52 if the guardrail adds an extension file).
- Enumerate every backticked `.../SKILL.md` reference under `skills/**` and show each resolves to a
  target whose `name` matches (expect 21 occurrences / 7 distinct paths).
- On a fixture repo **outside this repo**, probe all six skills with the `before_agent_start`
  technique: the four user-invoked ones (`grill-me`, `to-questionnaire`, `wait-what`, `teach`) must
  be **absent** from the rendered `<available_skills>` block but resolve as `/skill:<name>`; the two
  model-invoked ones (`writing-for-agents`, `git-guardrails`) must appear in the rendered block. Use
  the M4 fixture pattern and repair the backslash package path first.
- Run the user-invoked skills **live** where cheap (`/skill:wait-what`, `/skill:grill-me` are
  one-liners; `/skill:to-questionnaire` and `/skill:teach` write files). Report what ran live vs.
  structurally, and why.
- `git-guardrails`: whichever mechanism is chosen, demonstrate the guardrail blocking a real
  `git push` (and any other listed pattern) with the reason surfaced to the model, and demonstrate a
  safe git command still running. If the extension route: show it loads from the installed package
  and that the block produces `{ block: true, reason }`.
- `writing-for-agents`: prove no `CLAUDE.md` remains anywhere under `skills/**` and that the
  description names `AGENTS.md`.
- `teach`: prove `argument-hint` is gone and all four FORMAT sidecars exist and are referenced.
- Confirm `setup-pre-commit` was not ported and no `in-progress/*` skill was touched.

### Constraints and traps

- **Trap A — the frozen rename collides with the validator's verbatim-name check.** `git-guardrails`
  (`name: git-guardrails`) vs `upstreamName: git-guardrails-claude-code` fails check 7 with
  "D11 keeps names verbatim". The check is *wrong for the one rename D3/D11 explicitly authorize*,
  not merely inconvenient. Ask before changing it; the principled fix is to accept
  `row.renameTarget` as the expected name **only** when `disposition === "rename"`. Do not change the
  inventory, and do not leave the old name in the frontmatter (that violates D3).
- **Trap B — `git-guardrails` packaging.** The upstream mechanism is a Claude Code hook; Pi's is a
  `tool_call` extension. D1's manifest and §4's tree do not currently provide for one, yet §10
  schedules it. Ask before choosing (question 1 below).
- **Trap C — `writing-for-agents` contains the banned literal `CLAUDE.md` in its *description* and
  body.** The validator's no-allowlist ban catches it; rewrite to `AGENTS.md` in both places, and do
  not leave the old token in `SKILL-MECHANICS.md` either.
- **Trap D — `teach`'s `argument-hint`.** The validator hard-fails on it (D2/D5-18). Drop it; do not
  translate it into `metadata`.
- **Do not port `setup-pre-commit`** (D15). It has `disposition: "defer"` and
  `ownerMilestone: "post-v1"`; shipping it would also fail check 7's disposition rule.
- Do not add `docs/agents/`, `.claude-plugin/`, or `extensions/` unless the guardrail decision
  authorizes `extensions/`; if it does, that is a recorded, plan-scheduled deviation (§10), not a
  silent one.
- Do not rename any other skill or prefix `mp-` (D11). Names stay verbatim except the one frozen
  rename.

### Before you start, ask me

1. **`git-guardrails` packaging (Trap B).** Options:
   - **(A, recommended)** Ship `extensions/git-guardrails.ts` in this package (a `pi.on("tool_call")`
     blocker mirroring `permission-gate.ts`), add `"extensions": ["./extensions"]` to the `pi`
     manifest, and rewrite the skill so the user opts the guardrail in and can customize the pattern
     list. This is the only faithful port and it is what §10 schedules; it edits D1's literal
     manifest and adds `extensions/` to §4's tree, so it needs your sign-off as a recorded deviation.
   - **(B)** Keep D1 and §4 exactly: rewrite the skill as instructions for the user to add a
     `pi.on("tool_call")` hook to their **own** extension (pointing at `permission-gate.ts`), shipping
     no extension. No manifest change, but the skill then installs nothing.
   - **(C)** Port the skill + script only, with the mechanism left as a documented manual snippet, and
     defer the packaged extension to post-v1. Simplest, but it drops the §10 commitment.
   I recommend A: the plan already authorizes the extension in M5, and B/C leave the guardrail
   unenforced by default, which is the opposite of the skill's purpose.
2. **Validator rename carve-out (Trap A).** Options: **(A, recommended)** make check 7 expect
   `row.renameTarget` when `disposition === "rename"`, and only then — a correctness fix for the one
   frozen rename, with the negative case (a renamed row whose file still uses `upstreamName`) still
   failing; **(B)** leave the validator and record the rename as a known false positive. I recommend
   A; the ground rule says report a wrong check before changing it, which this question does.
3. **M5 acceptance live scope.** Options: **(A, recommended)** probe all six, run the four
   user-invoked skills live where cheap, and demonstrate the guardrail block live against a real
   `git push` in the fixture (needs the chosen extension active and the fixture repaired to
   forward-slash paths); **(B)** probe + structural verification only. I recommend A: the M3/M4 live
   runs were cheap and far more informative, and the guardrail is only meaningful if it actually
   blocks.

Non-blocking, please confirm the approach: `writing-for-agents`' "a hand-off or a subagent dispatch"
sentence. I plan to leave it as generic design prose rather than name the `subagent` tool, because it
names a context boundary rather than a call the agent must make — say if you want D5-04's tool name
inserted there instead.

Non-blocking and still open from earlier milestones: the GitHub owner `netname` in
`README.md`/`NOTICE`/`CHANGELOG.md` (NOTES §M0.5 item 1, unconfirmed by the owner); the
`pi-subagents` Windows backslash-path README troubleshooting entry (NOTES §M3.5), which is M6 work;
and the M6 re-test of `mp-evidence-auditor` against a discriminating claim (NOTES §M4.8). The README
skill index stays provisional and is finalized in M6.

### When done

Report in this shape:

- Files created/changed (paths + one-line purpose each)
- The validator result (file/`SKILL.md` counts) and the D5 freeze result
- The six ported skills, with per-file diff notes and the D4 enumeration (`grill-me`'s load)
- The `git-guardrails` rename resolution, the mechanism port, the packaging decision, and the live
  block evidence (including the validator change, if any, and why it is not a weakening)
- The `writing-for-agents` `CLAUDE.md` → `AGENTS.md` and invocation-mechanics reconciliation
- The `teach` `argument-hint` drop and sidecar verification
- The fixture-repo evidence: what ran live vs. structurally verified, with commands and outputs
- Every plan silence or error you hit, with what you did instead
- Confirmation that D1–D16 were not altered, and that `docs/ADAPTATION_PLAN.md` and the D5 table are
  byte-identical to `fca16af`
